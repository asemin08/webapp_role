---
# - name: Deploy a container
#   hosts: all
#   pre_tasks:
    
    # - name: install dependencies
    #   package:
    #     name: "{{ item }}"
    #     state: "present"
    #   loop:
    #     - epel-release
    #     - git
    #   when: ansible_distribution == "CentOS"

    # - name: "download Python-pip for docker module"
    #   get_url:
    #     url: https://bootstrap.pypa.io/pip/2.7/get-pip.py
    #     dest: /tmp/get-pip.py
 
    # - name: "install python-pip"
    #   command: python2.7 /tmp/get-pip.py
      
    # - name: Install docker python
    #   pip: name=docker-py

  # tasks: 
   
- name: "Create directory webapp"
  file:
    path: /home/{{ my_ansible_user }}/{{ instance_1.site_name }}
    state: directory
    
- name: "Download data from git repo"
  git:
    repo: 'https://github.com/diranetafen/static-website-example.git'
    dest: /home/{{ my_ansible_user }}/{{ instance_1.site_name }}

- name: "Copy new template"
  template: 		
    src: index.html.j2
    dest: /home/{{ my_ansible_user }}/{{ instance_1.site_name }}/index.html

- name: "launch docker container"
  docker_container:
    name: "{{ instance_1.container_name }}"
    image: httpd
    ports: 
      - "{{ instance_1.port }}:80"
    volumes:
      - /home/{{ my_ansible_user }}/{{ instance_1.site_name }}/:/usr/local/apache2/htdocs/:rw
